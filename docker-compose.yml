version: "3"

services:
  #Comp Prog Client App
  compprog-client:
    image: compprog-client
    ports:
      - 3000:3000
    stdin_open: true
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true